from setuptools import setup, find_packages

setup(
    name="wxCustomizableControls",
    version="0.0.1",
    packages=find_packages(),
    install_requires=["wxpython"],
    author="cenfra",
    description="A library containing customizable controls and windows for wxPython.",
    url="https://github.com/cenfraGit/wxCustomizableControls",
)
